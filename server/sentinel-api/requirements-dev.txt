# Test-only dependencies for server/sentinel-api/tests/. Not installed in the
# runtime Docker image (see Dockerfile) - these never ship to production.
#
# httpx is required by fastapi.testclient.TestClient but isn't a runtime
# dependency of the app itself, so it lives here rather than in the
# Dockerfile's pip installs.
pytest>=8.0.0,<9.0.0
httpx>=0.27.0,<1.0.0
